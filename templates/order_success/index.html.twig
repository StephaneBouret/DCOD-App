{% extends 'base.html.twig' %}

{% block title %}Confirmation de ma commande - Dis, comment on dit ?{% endblock %}
{% block stylesheets %}
<style type="text/css">
    body {
        position: relative;
        font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
        background-color: #FFFFFF;
        color: #212529;
    }

    header {
        height: 70px;
    }

    a {
        color: #e96b56;
        text-decoration: none;
    }
</style>
{% endblock %}
{% block body %}
<main id="main">
    <section id="check-order" class="check-order">
        <div class="container">
            <div class="section-title">
                <h2>Confirmation de commande</h2>
            </div>
        </div>
        <div class="row mt-4 justify-content-center">
            <div class="col-md-6 d-flex align-items-stretch">
                <div class="icon-box w-100">
                    <p>Bonjour <strong>{{ order.user.firstname }} {{ order.user.lastname }}</strong></p>
                    <p>Nous vous remercions pour votre commande n° <span
                            class="badge bg-secondary">{{ order.reference }}</span></p>
                    <p>Une confirmation vient de vous être envoyée par email à l'adresse suivante :
                        <strong>{{ order.user.email }}</strong></p>
                    <p>Vous pouvez dès à présent vous enregistrer sur l'application en cliquant sur le lien :
                        <a href="{{ path('register') }}">https://www.discommentondit.com/inscription</a>
                    </p>
                    <p><strong>L'équipe de Dis, comment on dit ?</strong></p>
                </div>
            </div>
        </div>
    </section>
</main>
{% endblock %}